[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "codelaw"
version = "0.1.0"
description = "Reproducible Legal Agent Harness A/B"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[tool.unittest]
start-directory = "tests"
